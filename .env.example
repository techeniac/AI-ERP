# Required for the AI Assistant chat feature (Platform + Org-level)
# Get your key at https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...

# If OPENAI_API_KEY is not set, the AI chat endpoint returns 500
# but all other pages (mock data) work fine without it.
